package Controller;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;

import javax.swing.JComponent;
import javax.swing.JOptionPane;
import javax.swing.SwingUtilities;

import view.Cadastro;
import view.Login;

public class MainController {
    private Login login;
    private Cadastro cadastro;

    public MainController(Login login, Cadastro cadastro) {
        this.login = login;
        this.cadastro = cadastro;

        /*login.addLoginListener(new LoginListener());
        cadastro.addCadastroListener(new CadastroListener());
    }/

    public class LoginListener implements ActionListener {
        @Override
        public void actionPerformed(ActionEvent e) {
            String email = login.getEmail();
            String senha = login.getSenha();

            // Verifique a autenticação usando algum serviço de autenticação ou lógica

          /*  boolean autenticacaoValida = false;
			if (autenticacaoValida) {
                // Abra a tela principal após o login
                TelaPrincipal telaPrincipal = new TelaPrincipal();
                telaPrincipal.setVisible(true);
                login.dispose(); // Feche a tela de login
            } else {*/
            
                JOptionPane.showMessageDialog(login, "Login inválido.");
            }
        }
   
    public class CadastroListener implements ActionListener {
        @Override
        public void actionPerformed(ActionEvent e) {
            JComponent cadastro;
			// Lógica para abrir a tela de cadastro
            cadastro.setVisible(true);
        }
   

    public static void main(String[] args) {
        SwingUtilities.invokeLater(() -> {
            Login login = new Login();
            Cadastro cadastro = new Cadastro();
            MainController controller = new MainController(login, cadastro);
            login.setVisible(true);
        });
    }
}
